# Security Model

- Local-first state and audit (`~/.onxity`).
- Permission scopes evaluated before each tool dispatch.
- Prompt/approval semantics for elevated scopes.
- Append-only audit log with redaction support.
- Risk budgets constrain plan steps and execution time.
- Capability packs require signed manifests and local staging.
